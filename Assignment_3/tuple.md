# Python এ Tuple - সম্পূর্ণ গাইড
## Tuple কি?

Tuple হলো Python এর একটি built-in ডেটা টাইপ যা **immutable** (অপরিবর্তনীয়)। এটি একাধিক ডেটা একসাথে রাখতে ব্যবহৃত হয়, তবে একবার তৈরি করার পর এটি পরিবর্তন করা যায় না।

## Tuple তৈরি করা

```python
# খালি tuple
empty_tuple = ()

# প্রথম বন্ধনী দিয়ে
fruits = ("আম", "কলা", "লিচু")

# বন্ধনী ছাড়াও (comma যথেষ্ট)
numbers = 1, 2, 3, 4, 5

# একটি মাত্র উপাদান দিয়ে tuple (comma লাগবে)
single = (5,)  # এটি tuple
not_tuple = (5)  # এটি integer

# tuple() constructor দিয়ে
my_tuple = tuple([1, 2, 3])
```

## Tuple এর বৈশিষ্ট্য

### ১. Immutable (অপরিবর্তনীয়)
```python
fruits = ("আম", "কলা", "লিচু")
# fruits[0] = "জাম"  # Error হবে!
```

### ২. Ordered (ক্রমানুসারে)
Tuple এর উপাদানগুলো একটি নির্দিষ্ট ক্রমে থাকে।

### ৩. Indexing সাপোর্ট করে
```python
fruits = ("আম", "কলা", "লিচু", "পেয়ারা")
print(fruits[0])   # আম
print(fruits[-1])  # পেয়ারা (শেষ থেকে)
```

### ৪. Duplicate উপাদান রাখতে পারে
```python
numbers = (1, 2, 2, 3, 3, 3)
```

## Tuple Operations

### Slicing (অংশ কাটা)
```python
fruits = ("আম", "কলা", "লিচু", "পেয়ারা", "জাম")
print(fruits[1:4])    # ('কলা', 'লিচু', 'পেয়ারা')
print(fruits[:3])     # ('আম', 'কলা', 'লিচু')
print(fruits[2:])     # ('লিচু', 'পেয়ারা', 'জাম')
print(fruits[::2])    # ('আম', 'লিচু', 'জাম')
```

### Concatenation (যোগ করা)
```python
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
result = tuple1 + tuple2  # (1, 2, 3, 4, 5, 6)
```

### Repetition (পুনরাবৃত্তি)
```python
my_tuple = ("হ্যালো",) * 3
print(my_tuple)  # ('হ্যালো', 'হ্যালো', 'হ্যালো')
```

### Membership (সদস্যতা চেক)
```python
fruits = ("আম", "কলা", "লিচু")
print("আম" in fruits)      # True
print("জাম" not in fruits)  # True
```

## Tuple Methods

Python এ Tuple এর মাত্র ২টি method আছে:

### ১. count() - কতবার আছে গণনা করে
```python
numbers = (1, 2, 2, 3, 2, 4)
print(numbers.count(2))  # 3
```

### ২. index() - প্রথম occurrence এর index খুঁজে
```python
fruits = ("আম", "কলা", "লিচু", "কলা")
print(fruits.index("কলা"))  # 1
```

## Tuple Unpacking (আনপ্যাক করা)

```python
# সাধারণ unpacking
person = ("রহিম", 25, "ঢাকা")
name, age, city = person
print(name)  # রহিম

# Asterisk (*) দিয়ে
numbers = (1, 2, 3, 4, 5)
first, *middle, last = numbers
print(first)   # 1
print(middle)  # [2, 3, 4]
print(last)    # 5
```

## Nested Tuple (নেস্টেড টিউপল)

```python
nested = ((1, 2), (3, 4), (5, 6))
print(nested[0])     # (1, 2)
print(nested[1][0])  # 3
```

## Built-in Functions

```python
numbers = (5, 2, 8, 1, 9)

len(numbers)    # 5 (দৈর্ঘ্য)
max(numbers)    # 9 (সর্বোচ্চ)
min(numbers)    # 1 (সর্বনিম্ন)
sum(numbers)    # 25 (যোগফল)
sorted(numbers) # [1, 2, 5, 8, 9] (list return করে)
```

## Tuple vs List - পার্থক্য
---------------------------------------------------------
|       Tuple                             |       List                    |
|-----------------------------------------|-------------------------------|
| Immutable (পরিবর্তন করা যায় না)           | Mutable (পরিবর্তন করা যায়)     |
| `()` বন্ধনী                               | `[]` বন্ধনী                     |
| দ্রুত (faster)                            | তুলনামূলক ধীর                  |
| কম memory ব্যবহার করে                   | বেশি memory                    |
| নিরাপদ data এর জন্য                     | পরিবর্তনশীল data এর জন্য        |

## কখন Tuple ব্যবহার করবেন?

1. **অপরিবর্তনীয় ডেটা**: যখন data পরিবর্তন করার প্রয়োজন নেই
2. **Dictionary এর key হিসেবে**: List key হিসেবে ব্যবহার করা যায় না, কিন্তু Tuple যায়
3. **Multiple values return করতে**: Function থেকে একাধিক মান ফেরত দিতে
4. **Performance**: List এর চেয়ে দ্রুত

## উদাহরণ: বাস্তব ব্যবহার

```python
# ১. Geographic coordinates
location = (23.8103, 90.4125)  # Dhaka এর lat, long

# ২. RGB color
red = (255, 0, 0)

# ৩. Function থেকে multiple return
def get_student_info():
    return ("রহিম", 25, "CSE")

name, age, dept = get_student_info()

# ৪. Dictionary key হিসেবে
scores = {
    ("Rahim", "Math"): 95,
    ("Karim", "English"): 88
}
```

## গুরুত্বপূর্ণ বিষয়

- Tuple immutable কিন্তু যদি এর ভিতরে mutable object (যেমন list) থাকে, সেটি পরিবর্তন করা যায়
```python
my_tuple = (1, 2, [3, 4])
my_tuple[2].append(5)  # এটি কাজ করবে!
print(my_tuple)  # (1, 2, [3, 4, 5])
```


List এবং Tuple এর মধ্যে **similarity প্রায় 70-75%** বলা যায়। চলুন বিস্তারিত দেখি:

## ✅ Similarities (মিল) - যা একই

### 1. **Sequential/Ordered Collection**
উভয়ই ordered collection, মানে elements একটি নির্দিষ্ট ক্রমে থাকে।

```python
my_list = [1, 2, 3]
my_tuple = (1, 2, 3)
# উভয়ই ক্রমানুসারে
```

### 2. **Indexing এবং Slicing**
```python
my_list = ["আম", "কলা", "লিচু"]
my_tuple = ("আম", "কলা", "লিচু")

# উভয়ই একইভাবে কাজ করে
print(my_list[0])      # আম
print(my_tuple[0])     # আম

print(my_list[1:])     # ['কলা', 'লিচু']
print(my_tuple[1:])    # ('কলা', 'লিচু')
```

### 3. **Duplicate Elements রাখতে পারে**
```python
my_list = [1, 2, 2, 3]
my_tuple = (1, 2, 2, 3)
# উভয়ই duplicate allow করে
```

### 4. **Heterogeneous Data (বিভিন্ন ধরনের ডেটা)**
```python
my_list = [1, "hello", 3.14, True]
my_tuple = (1, "hello", 3.14, True)
# উভয়ই mixed data type রাখতে পারে
```

### 5. **Iteration (লুপ চালানো)**
```python
for item in [1, 2, 3]:
    print(item)

for item in (1, 2, 3):
    print(item)
# উভয়ই একইভাবে iterate হয়
```

### 6. **Common Built-in Functions**
```python
my_list = [1, 2, 3, 4, 5]
my_tuple = (1, 2, 3, 4, 5)

len(my_list)    # 5
len(my_tuple)   # 5

max(my_list)    # 5
max(my_tuple)   # 5

min(my_list)    # 1
min(my_tuple)   # 1

sum(my_list)    # 15
sum(my_tuple)   # 15
```

### 7. **Membership Testing**
```python
print(2 in [1, 2, 3])      # True
print(2 in (1, 2, 3))      # True
```

### 8. **Concatenation (+)**
```python
list1 = [1, 2] + [3, 4]      # [1, 2, 3, 4]
tuple1 = (1, 2) + (3, 4)     # (1, 2, 3, 4)
```

### 9. **Repetition (*)**
```python
my_list = [1, 2] * 3         # [1, 2, 1, 2, 1, 2]
my_tuple = (1, 2) * 3        # (1, 2, 1, 2, 1, 2)
```

### 10. **Nesting (নেস্টিং)**
```python
nested_list = [[1, 2], [3, 4]]
nested_tuple = ((1, 2), (3, 4))
# উভয়ই nested হতে পারে
```

### 11. **Unpacking**
```python
a, b, c = [1, 2, 3]
x, y, z = (1, 2, 3)
# উভয়ই unpack করা যায়
```

## ❌ Differences (পার্থক্য) - যা আলাদা

### 1. **Mutability (সবচেয়ে বড় পার্থক্য)**
```python
my_list = [1, 2, 3]
my_list[0] = 10          # কাজ করবে ✅

my_tuple = (1, 2, 3)
# my_tuple[0] = 10       # Error হবে ❌
```

### 2. **Methods এর সংখ্যা**
```python
# List: অনেক methods
my_list.append()
my_list.remove()
my_list.insert()
my_list.sort()
my_list.reverse()
# আরও অনেক...

# Tuple: মাত্র 2টি methods
my_tuple.count()
my_tuple.index()
```

### 3. **Syntax (বন্ধনী)**
```python
my_list = [1, 2, 3]      # Square brackets
my_tuple = (1, 2, 3)     # Parentheses
```

### 4. **Performance**
```python
import sys

my_list = [1, 2, 3, 4, 5]
my_tuple = (1, 2, 3, 4, 5)

print(sys.getsizeof(my_list))   # 104 bytes
print(sys.getsizeof(my_tuple))  # 80 bytes
# Tuple কম memory ব্যবহার করে
```

### 5. **Dictionary Key**
```python
# Tuple key হিসেবে ব্যবহার করা যায়
my_dict = {(1, 2): "value"}  # ✅

# List key হিসেবে ব্যবহার করা যায় না
# my_dict = {[1, 2]: "value"}  # ❌ Error
```

## 📊 Similarity Breakdown:

| Feature | List | Tuple | Match? |
|---------|------|-------|--------|
| Ordered | ✅   | ✅   | ✅     |
| Indexed | ✅   | ✅   | ✅     |
| Slicing | ✅   | ✅   | ✅     |
| Duplicates|✅  | ✅   | ✅     |
| Heterogeneous | ✅ | ✅ | ✅ |
| Iterable | ✅ | ✅ | ✅ |
| Concatenation | ✅ | ✅ | ✅ |
| Repetition | ✅ | ✅ | ✅ |
| Nesting | ✅ | ✅ | ✅ |
| **Mutable** | ✅ | ❌ | ❌ |
| **Methods** | Many | 2 | ❌ |
| **Memory** | More | Less | ❌ |
| **Speed** | Slower | Faster | ❌ |
| **Dict Key** | ❌ | ✅ | ❌ |

## 🎯 সারসংক্ষেপ:

**মিল আছে**: প্রায় **70-75%**
- Basic structure এবং behavior প্রায় একই
- Access, iteration, এবং common operations একই

**পার্থক্য**: প্রায় **25-30%**
- মূল পার্থক্য হলো **mutability**
- এই একটি পার্থক্যই বাকি সব পার্থক্যের কারণ

**সহজ কথায়**: List এবং Tuple হলো **twin brothers** যাদের মূল পার্থক্য হলো একজন পরিবর্তনশীল (List), আরেকজন অপরিবর্তনশীল (Tuple)। বাকি সব আচরণ প্রায় একই!